pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh 'echo "Hello CloudBabies!!!"'
                sh '''
                    echo "Welcome to Jenkins..."
                    ls -lah
                '''
            }
        }
    }
}
